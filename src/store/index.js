import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const store = new Vuex.store({
  strict: true,

  state:{},
  mutations:{},
  actions:{},
  getters:{},
  modules:{}
});

export default store;
